# Postgres connection string used by Prisma (packages/db)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ondo?schema=public"

# Port the Express API listens on (apps/api)
PORT=4000

# Base URL the web app uses to reach the API (apps/web)
NEXT_PUBLIC_API_URL="http://localhost:4000"
